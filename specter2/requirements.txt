# Pinned, mutually-compatible combo for the SPECTER2 adapters stack.
# `adapters` is the successor to adapter-transformers and is version-coupled
# to `transformers`. Keep these aligned with the Databricks serverless AI
# environment; older 1.0.x adapter stacks fail more often on serverless.
torch==2.9.0
transformers==4.57.6
adapters==1.3.0
pandas==2.2.2
numpy==1.26.4
